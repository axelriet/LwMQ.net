version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
    nodejs: "16"
  jobs:
    post_install:
      - npm install -g @mermaid-js/mermaid-cli

python:
  install:
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/conf.py
